# GitHub Personal Access Token (optional — for GraphQL pinned repos + higher rate limits)
VITE_GITHUB_TOKEN=

# Anthropic API Key (for Claude-generated specimen descriptions)
VITE_ANTHROPIC_API_KEY=

# Optional: proxy URL for Claude API calls (if you want to avoid CORS via a backend)
VITE_CLAUDE_PROXY_URL=
